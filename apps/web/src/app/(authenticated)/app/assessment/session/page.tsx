import { completeDiagnostic, submitDiagnosticAnswer } from '@/features/assessments/actions';
import { AssessmentPage } from '@/features/assessments/components/adaptive-page';
import { nextQuestion } from '@/features/assessments/server/adaptive-assessment';
export default async function SessionPage({searchParams}:{searchParams:Promise<{id?:string}>}){const id=(await searchParams).id;if(!id)return <AssessmentPage title="Sessão" description="Avaliação não informada."><p>Inicie um diagnóstico.</p></AssessmentPage>;const question=await nextQuestion(id);if(!question)return <AssessmentPage title="Questões concluídas" description="Finalize para persistir o relatório diagnóstico."><form action={completeDiagnostic}><input type="hidden" name="assessmentId" value={id}/><button className="rounded-lg bg-[var(--color-accent)] px-4 py-2 text-white">Concluir avaliação</button></form></AssessmentPage>;return <AssessmentPage title={`Questão ${question.number} de ${question.total}`} description={question.selectionReason}><form action={submitDiagnosticAnswer} className="space-y-5"><input type="hidden" name="assessmentId" value={id}/><input type="hidden" name="questionVersionId" value={question.questionVersionId}/><input type="hidden" name="responseTimeMs" value="30000"/><fieldset><legend className="mb-4 text-lg font-medium">{question.stem}</legend>{question.options.map(option=><label key={option.id} className="mb-2 flex gap-3 rounded-lg border border-[var(--color-border)] p-3"><input required type="radio" name="selectedOptionId" value={option.id}/><span><strong>{option.label}.</strong> {option.content}</span></label>)}</fieldset><label>Confiança informada <select name="statedConfidence" className="ml-2 rounded border p-2"><option value="low">Baixa</option><option value="medium">Média</option><option value="high">Alta</option></select></label><button className="block rounded-lg bg-[var(--color-accent)] px-4 py-2 text-white">Responder e adaptar</button></form></AssessmentPage>}
