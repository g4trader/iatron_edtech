export default function AdminAuditPage() {
  return (
    <main className="experience-page mx-auto w-full max-w-5xl space-y-4 px-4 py-8 sm:p-8">
      <h1 className="text-3xl font-semibold">Auditoria editorial</h1>
      <p>
        Consulte o histórico a partir de cada conteúdo. Os registros são
        append-only e não podem ser alterados por esta interface.
      </p>
    </main>
  );
}
